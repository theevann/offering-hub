API LOGGER
Build a logger for api that includes:
 llm request - llm response
 incoming requests
 secure the endpoints (groups, ingest)
 Don't create raw messages for duplicates


BOT 
- replay log for messages failed to be sent to API


LLM CALL
* - Handle images (simple local OCR ?)
* - Save links (manual parsing)
* - Handle multi days event
* - Handle message offering recurrent events (only add them for the next week) 
* - Handle One Raw -> Many Offerings
* - Add fallback llm
* - Multi stage parsing:
    1. Is it an offering (cheap model YES/NO + CATEGORY) ? This should be a per group setting (if group tends to have a lot of chitchat then precheck=true)
    2. If exact duplicate or num_char < 50 then no parsing
    3. Parse it: DONE
    4. Check for deduplication if (date, time, category) matches: DONE


FRONTEND:
- location first experience ... we need to implement search by location : this would be a serverside filtering first -- need reflexion on the best UI for that

- created at time